# Module 1 - Language Adapter Dependencies

# Protocol Buffers and gRPC
grpcio>=1.60.0
grpcio-tools>=1.60.0
protobuf>=4.25.0

# LSP Support
pygls>=1.3.0

# Python AST parsing (built-in, but listing for clarity)
# ast (standard library)

# Logging and utilities
python-dotenv>=1.0.0