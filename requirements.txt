# ─────────────────────────────────────────────────────────────────────────────
# Python dependencies for Module 1 (Language Adapter / gRPC server)
#
# Install:  pip install -r requirements.txt
#
# The Rust workspace (Modules 2-4, api_server) is built with:
#   cargo build --release
#
# The React UI is set up with:
#   cd ui && npm install
# ─────────────────────────────────────────────────────────────────────────────

# gRPC and Protocol Buffers
grpcio>=1.60.0
grpcio-tools>=1.60.0
protobuf>=4.25.0

# Multi-language AST parsing (Tree-sitter)
tree-sitter>=0.21.0
tree-sitter-languages>=1.10.0

# LSP (Language Server Protocol) support
pygls>=1.3.0
lsprotocol>=2.0.0
